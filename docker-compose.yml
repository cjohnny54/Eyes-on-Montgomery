version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:3000"
    env_file:
      - .env.local
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true # Helps with file watching on Windows/WSL
